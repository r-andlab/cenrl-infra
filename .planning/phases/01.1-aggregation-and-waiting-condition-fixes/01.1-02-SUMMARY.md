---
phase: 01.1-aggregation-and-waiting-condition-fixes
plan: 02
subsystem: infrastructure
tags: [orchestrator, aggregator-wiring, vp-removal, debug-poll, resend]
dependency_graph:
  requires: [register_targets, remove_vantage_points, call_debug_endpoint]
  provides: [orchestrator-register-targets-wiring, vp-removal-on-failure, debug-poll-resend]
  affects: [measurement-lifecycle, vp-lifecycle, stuck-measurement-recovery]
tech_stack:
  added: []
  patterns: [single-debug-call-per-cycle, lock-then-release-before-http, batch-vp-removal]
key_files:
  created: []
  modified:
    - Infrastructure/main/orchestrator.py
    - tests/infrastructure/test_orchestrator.py
decisions:
  - "Aggregator lock released before HTTP resend calls to prevent deadlock"
  - "Single /debug call per check cycle to prevent request amplification"
  - "Failed eval VPs batch-removed at end of _process_eval_results loop"
  - "Health-check VPs removed individually at threshold breach"
  - "Pre-existing test failures fixed inline (wrong _last_delay_warn type, missing mock attrs)"
metrics:
  duration: "4m 38s"
  completed: "2026-04-23T21:37:00Z"
  tasks: 2
  files_changed: 2
requirements_completed: [D-03, D-04, D-05, D-06, D-09, D-10, D-11, D-12]
---

# Phase 01.1 Plan 02: Orchestrator Wiring and /debug Poll Resend Summary

Wire register_targets at scheduling site replacing update_aggregator_vps, batch-remove failed VPs from Hyperquack, add /debug poll with single call per cycle to detect and resend lost measurements to original or alternative VP.

## Task Results

### Task 1: Wire register_targets at scheduling site and add VP removal calls

**Commit:** `1fcdda6`

**Changes:**
- Replaced `self.api.update_aggregator_vps(country, set(active_vps))` with `self.api.aggregator.register_targets(country, targets, set(active_vps))` in tick() scheduling block
- Added `failed_vps: List[str] = []` accumulator in `_process_eval_results()` with `failed_vps.append(vp)` on eval failure
- Added `self.api.remove_vantage_points(failed_vps)` batch call after eval processing loop
- Added `self.api.remove_vantage_points([r.vp])` per-VP call in `_check_vp_health()` after threshold breach

### Task 2: Add /debug poll and resend logic for stuck measurements (TDD)

**Commits:**
- `a8aad92` test(01.1-02): add failing tests for /debug poll, resend, and wiring verification (RED)
- `b49bf65` feat(01.1-02): add /debug poll and resend logic for stuck measurements (GREEN)

**Changes:**
- Extended `_check_delayed_results()` to collect stuck targets into `stuck_by_country` dict and call `_check_and_resend_stuck()`
- Added `_check_and_resend_stuck()` method: debug mode guard, single `/debug` call per cycle, builds VP status lookup, reads aggregator state under lock, resends outside lock
- Resend logic: skips targets still in VP's unstarted_work, resends to original VP if not pending_removal, uses alternative VP if pending_removal
- Added 12 new test cases across 4 test classes (TestRegisterTargetsWiring, TestVPRemovalOnEvalFailure, TestVPRemovalOnHealthCheck, TestCheckAndResendStuck)
- Fixed 3 pre-existing test failures: `_last_delay_warn` was set as float instead of defaultdict(dict), missing mock attributes

## TDD Gate Compliance

- RED gate: `a8aad92` -- 10 new tests confirmed failing before implementation
- GREEN gate: `b49bf65` -- all 22 orchestrator tests pass, all 46 infrastructure tests pass
- REFACTOR gate: not needed, code is clean

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed pre-existing test setup for _last_delay_warn**
- **Found during:** Task 2
- **Issue:** Old test helpers in TestRunForeverLoopCondition, TestDailyResetTrigger, TestSignalShutdown set `_last_delay_warn = time.monotonic()` (a float) instead of `defaultdict(dict)`, causing AttributeError on `.clear()` and `.get()`
- **Fix:** Changed all instances to `defaultdict(dict)` and added missing import
- **Files modified:** tests/infrastructure/test_orchestrator.py
- **Commit:** b49bf65

**2. [Rule 1 - Bug] Fixed test_no_remove_when_all_pass missing node context**
- **Found during:** Task 2
- **Issue:** Test created a passing eval payload but orchestrator tried to create a real RegionalNode (needing self.params), causing AttributeError
- **Fix:** Pre-created a mock node in orch.agents so node creation is skipped
- **Files modified:** tests/infrastructure/test_orchestrator.py
- **Commit:** b49bf65

## Known Stubs

None.

## Verification

```
tests/infrastructure/test_aggregator.py: 8 passed
tests/infrastructure/test_funneler.py: 7 passed
tests/infrastructure/test_node.py: 9 passed
tests/infrastructure/test_orchestrator.py: 22 passed
Total: 46 passed, 0 failed
```

## Self-Check: PASSED
